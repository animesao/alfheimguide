# 🌟 Multi-Functional Discord Bot (RU/EN)

Универсальный Discord бот с открытым исходным кодом, написанный на Python с использованием библиотек `discord.py`, `PyGithub`. Предназначен для управления серверами, автоматизации, модерации и взаимодействия с комьюнити.

## 🚀 Основные возможности

### 🛠️ Управление и Настройка
- **Мультисерверность**: Независимые настройки для каждого сервера.
- **Локализация**: Полная поддержка русского (`ru`) и английского (`en`) языков.
- **Гибкий конфиг**: Настройка префикса, каналов и сообщений через `/config`.
- **База данных**: Поддержка **SQLite** (по умолчанию) и **MySQL**.

### 🛡️ Модерация и Безопасность
- **Автомодерация**: Фильтр плохих слов с возможностью настройки собственного списка.
- **Команды управления**: Быстрые `kick`, `ban` и `clear` через Slash-команды.

### 📈 Система Уровней (XP)
- **Начисление опыта**: Пользователи получают XP за общение.
- **Кастомизация**: Настраиваемые сообщения о повышении уровня с поддержкой изображений.
- **XP Rate**: Возможность изменять скорость получения опыта для сервера.

### 🎫 Система Тикетов
- **Интерактивность**: Открытие тикетов через кнопки.
- **Организация**: Автоматическое создание каналов в заданных категориях.
- **Приватность**: Настройка прав доступа для пользователя и администрации.
- **Кастомизация**: Настройка заголовков, текста кнопок и приветственных сообщений.

### 🐙 Интеграция с GitHub
- **Трекинг пользователей**: Отслеживание публичных событий (коммитов, пушей) конкретных пользователей.
- **Уведомления**: Гибкие шаблоны сообщений о новых событиях в заданные каналы.

### 🎉 Розыгрыши (Giveaways)
- **Простота запуска**: Создание розыгрышей через `/giveaway`.
- **Визуализация**: Поддержка изображений призов и кастомных описаний через шаблоны.

---

## 🛠 Установка и Запуск

### Требования
- Python 3.10+
- Токен Discord бота ([Discord Developer Portal](https://discord.com/developers/applications))

### Быстрый старт
1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/yourusername/discord-bot.git
   cd discord-bot
   ```
2. Установите зависимости:
   ```bash
   pip install discord.py mysql-connector-python aiosqlite aiohttp python-dotenv
   ```
3. Настройте `config.json`:
   ```json
   {
     "DISCORD_TOKEN": "ВАШ_ТОКЕН",
     "DB_TYPE": "sqlite",
     "DB_HOST": "localhost",
     "DB_USER": "root",
     "DB_PASS": "",
     "DB_NAME": "bot_db"
   }
   ```
4. Запустите бота:
   ```bash
   python main.py
   ```

---

## 📋 Список Команд (Slash Commands)

| Команда | Описание |
| :--- | :--- |
| `/config` | Настройка параметров сервера (язык, каналы, сообщения, изображения) |
| `/rank` | Просмотр текущего уровня, опыта и прогресса |
| `/kick` | Исключение пользователя с сервера |
| `/ban` | Блокировка пользователя на сервере |
| `/clear` | Очистка указанного количества сообщений |
| `/track_github` | Настройка отслеживания GitHub пользователя в канале |
| `/giveaway` | Запуск нового розыгрыша с картинкой и временем |
| `/setup_tickets` | Установка интерактивной панели для создания тикетов |

---

## ⚙️ Переменные шаблонов
В настройках сообщений можно использовать динамические переменные:
- `{user}` — упоминание пользователя.
- `{guild}` — название сервера.
- `{level}` — достигнутый уровень.
- `{repo}` — название репозитория (для GitHub).
- `{prize}`, `{duration}`, `{winners}` — для розыгрышей.

---

## 🤝 Вклад в разработку
Будем рады вашим Pull Request и сообщениям об ошибках в разделе Issues!
Также будем рады если зайдёти к нам на дс сервер!

---
